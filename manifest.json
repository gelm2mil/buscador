let datos = [];

fetch("placas.json")
    .then(res => res.json())
    .then(json => {
        datos = json;
        document.getElementById("estado").textContent = "Archivo cargado ✔️";
    })
    .catch(() => {
        document.getElementById("estado").textContent = "Error cargando información ❌";
    });
